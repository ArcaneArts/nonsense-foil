name: foil_demo
description: A demonstration of wrapping a widget with Foil.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ^3.7.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  spectrum: 0.2.1+1

dev_dependencies:
  nonsense_foil:
    path: ../

flutter:
  uses-material-design: true
